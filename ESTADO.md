# ESTADO — DuoSync Wallet
Última actualización: 2026-08-27 | Sesión actual: 6

⏸️ CHECKPOINT — Sesión 5 CERRADA: app interna construida (Hoy/Gastos/Metas/Nosotros), pulida con varias rondas de ajustes de diseño a pedido del usuario (logo real integrado, menú flotante, colores de categoría, profundidad de tarjetas) — detalle completo en las secciones de abajo. Sesión 6 EN CURSO — servicios externos, siguiendo `docs/sistema/62-PUBLICACION-SEGURA-Y-CONTINUA.md` (protocolo P0-P9) + `SECUENCIA-MAESTRA-CONSTRUCCION.md` §Paso 6 (orden: Git/GitHub → Supabase → IA real → Vercel → Resend → dominio → Hotmart).
- Git/GitHub: HECHO (2026-08-23). Repo inicializado en la raíz del proyecto, `.gitignore` verificado (sin secretos, sin node_modules), primer commit (300 archivos) empujado a `https://github.com/ideasmart25m-maker/duosync.git`, rama `main`. SHA local y remoto coinciden (235e8e4). El usuario tuvo que correr el `push` desde su propia terminal porque requería login interactivo en el navegador (Git Credential Manager) que mi entorno no puede mostrar.
- Supabase: HECHO (2026-08-24). Proyecto "Duosync Wallet" (ref `bhpiqvvspxnmztjgjxhx`, región us-east-1) enlazado vía `supabase link` (login CLI por navegador, corrido por el usuario en su propia terminal). Esquema inicial aplicado con 2 migraciones: `20260824193739_esquema_inicial.sql` (profiles, couples, couple_members, categories, expenses, receipt_scans, savings_goals, daily_questions, daily_answers, streaks, dashboard_layout — RLS activo en TODAS, patrón couple_id+couple_members de `25-BASE-DE-DATOS.md`; RPCs `crear_pareja()` y `unirse_con_codigo()` para las operaciones privilegiadas de vinculación, en vez de inserts libres desde el cliente) y `20260824193929_preguntas_diarias_seed.sql` (7 preguntas diarias sembradas). `supabase migration list` confirma local=remoto.
- ✅ INCIDENTE DE SEGURIDAD CERRADO (2026-08-24): al pedir la clave pública (publishable) vía `supabase projects api-keys`, el comando también imprimió en texto plano la clave `service_role` LEGACY del proyecto (secreta, bypassa RLS). Se le avisó al usuario de inmediato; confirmó haber deshabilitado las Legacy API Keys (anon + service_role) desde Project Settings → API Keys → Legacy API Keys en Supabase. La clave nunca se usó en ningún código. Lección para la próxima vez: pedir la publishable key por captura/copia manual del usuario, nunca con un comando que también devuelva las secretas.
- ✅ RESUELTO (2026-08-26, auditoría senior): `unirse_con_codigo()` ya tiene límite de intentos — migración `20260826113231_limite_intentos_union.sql` agrega `private.intentos_union` (bloqueo de 15 min tras 5 intentos fallidos por usuario, se resetea al acertar). Aplicada y confirmada con `db push`.
- ⚠️ Pendiente de producto: la mecánica "no se ve la respuesta del otro hasta que ambos respondan" NO está resuelta a nivel de base de datos (RLS controla qué FILAS existen, no puede ocultar el contenido de una fila ya visible) — falta una vista o RPC que la resuelva cuando se conecten las pantallas reales a Supabase (todavía usan datos de ejemplo locales).
- `app/.env.local` HECHO (2026-08-24): tiene `NEXT_PUBLIC_SUPABASE_URL` y `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (ambas públicas) ya cargadas — no se le pidió nada al usuario, son valores no secretos. `app/.env.example` creado como plantilla (solo nombres) y SÍ se versiona — se corrigió `app/.gitignore` (regla `.env*` + excepción `!.env.example`) para que la plantilla se pueda subir a GitHub sin subir `.env.local`. `SUPABASE_SECRET_KEY` queda sin usar por ahora (ningún código la necesita todavía).
- Cliente de Supabase HECHO (2026-08-24): `@supabase/supabase-js` + `@supabase/ssr` instalados en `app/`. Creados `app/lib/supabase/client.ts` (navegador) y `app/lib/supabase/server.ts` (Server Components/Actions) — ambos usan solo las claves públicas. Creado `app/proxy.ts` (Next.js 16 renombró `middleware.ts` → `proxy.ts`/función `proxy` — se usó la convención nueva directamente, sin generar deuda técnica) que refresca la sesión en cada request. tsc ✓ build ✓ dev probado a mano (landing y `/app/hoy` cargan sin errores en consola con el proxy activo).
- Vercel: HECHO (2026-08-25). Proyecto importado desde el dashboard de Vercel (vercel.com/new) conectado al repo de GitHub — Root Directory = `app` (el código Next.js vive en la subcarpeta, no en la raíz del repo). Variables de entorno públicas (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`) cargadas en Production+Preview. Primer despliegue Ready en `https://duosync-jfr5.vercel.app`. Conexión Git→Vercel VERIFICADA de verdad (no solo un deploy manual): 2 pushes reales desde la terminal del usuario dispararon auto-deploy sin tocar nada en Vercel (doctrina `62` P5 — la prueba que sí cuenta).
- 🐛 BUG REAL encontrado y corregido en producción (2026-08-25): la pantalla Hoy (`app/app/app/hoy/page.tsx`) tiraba un error de hidratación de React (#418) SOLO en Vercel, nunca en localhost — el saludo ("Buenos días/tardes/noches") se calculaba con la hora del SERVIDOR, que en producción está en otra zona horaria que el navegador del usuario, así que el texto no coincidía entre el HTML renderizado en el servidor y lo que React esperaba al hidratar en el cliente. Corregido: el saludo ahora se calcula solo en el cliente después de montar (arranca en "Hola" neutro, se actualiza con `useEffect`). Verificado en el sitio publicado: 0 errores de consola tras el segundo despliegue. Lección: los bugs de hidratación por reloj/zona horaria/`Math.random()`/`window` solo se manifiestan con servidor y cliente en momentos o lugares distintos — nunca se ven en local y hay que probarlos en el dominio real.
- Nota técnica: Next.js 16 renombró `middleware.ts` → `proxy.ts` (función `proxy`, no `middleware`) — ya se usó la convención nueva desde el principio.
## Auditoría senior (2026-08-26) — Capa 1: seguridad y backend ✅
Reporte completo entregado y aprobado por el usuario ("Apruebo todo"). Puntaje de partida: 5.5/10 — app visualmente terminada pero con el backend de Sesión 6 totalmente desconectado del frontend. Ejecutado:
- **Headers de seguridad**: agregado `headers()` en `app/next.config.ts` — CSP (con `connect-src` apuntando a Supabase), X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, Strict-Transport-Security. Verificado con `curl -I` en local: los 6 headers presentes.
- **Límite de intentos** en `unirse_con_codigo()` — ver línea de Supabase arriba.
- **Auth real conectada** (el hallazgo crítico #1 de la auditoría): `app/app/login/page.tsx` ahora llama de verdad a `supabase.auth.signInWithOtp()` (antes simulaba con `setTimeout`), con estados de carga y error reales (antes "cero estados de error en toda la app" — ya no). Creado `app/app/auth/callback/route.ts`: recibe el enlace mágico, confirma la sesión, y llama a `crear_pareja()` o `unirse_con_codigo()` según el modo — el código/modo de vinculación viaja por la URL desde el onboarding (`PasoResultado`) → paywall → login → callback (antes se perdía, era 100% estado local).
- **Rutas protegidas**: `app/proxy.ts` ahora redirige a `/login` cualquier intento de entrar a `/app/*` sin sesión real — antes cualquiera entraba sin registrarse.
- **Verificado de verdad, no solo "compila"**: `curl -I` confirmó los 6 headers; navegar a `/app/hoy` sin sesión SÍ redirige a `/login?plan=free` (probado); el envío del magic link SÍ llega a la API real de Supabase (probado con fetch directo — Supabase respondió `email_address_invalid` para un dominio de prueba bloqueado, y `over_email_send_rate_limit` para un correo válido, confirmando que la petición se procesa de verdad; no se pudo confirmar la recepción del correo porque Supabase con proveedor de email por defecto tiene un límite muy bajo de envíos — normal, se resuelve conectando Resend, ya en la lista de próximos pasos). tsc ✓ build ✓ (`/auth/callback` aparece como ruta dinámica).
- ⚠️ Alcance deliberado: las pantallas Hoy/Gastos/Metas/Nosotros TODAVÍA muestran datos de ejemplo (`seed-datos.ts`), no datos reales de Supabase — conectar el CRUD completo de las 4 pantallas es una capa aparte, más grande, que no se mezcló con esta (evita un cambio gigante sin revisar). Auth y vinculación de pareja SÍ son reales ahora; el contenido del día a día, todavía no.
## Auditoría senior (2026-08-26) — Capa 3: diseño ✅
- **Login** (`app/app/login/page.tsx`): logo real reemplazó el cuadrado de color placeholder (mismo patrón `Link`+`Image` que onboarding/paywall); vacío muerto (~45% del viewport) corregido con el mismo `flex-1 justify-center` ya probado en el onboarding.
- **Metas** (`app/app/app/metas/page.tsx`): vacío muerto (~40% del viewport debajo de "Nueva meta juntos") corregido con el mismo patrón.
- Verificado con capturas reales a 375px: `docs/revisiones/auditoria-login-fix-375.png`, `docs/revisiones/auditoria-metas-fix-375.png` (la de Metas requirió desactivar la protección de rutas nueva de la Capa 1 temporalmente y en local para poder verla sin sesión real — se restauró y se reconfirmó el redirect antes de seguir). tsc ✓ build ✓.
- Auditoría CERRADA: los 2 hallazgos 🔴 crítico y los 3 🟠 importante del reporte quedaron resueltos. Pendiente deliberado (no crítico, ya explicado arriba): el CRUD completo de la app interna sigue en datos de ejemplo, y el límite de 15 movimientos/mes del plan gratis no se verifica en código todavía — ambos dependen de la misma conexión de datos real, que es la siguiente pieza natural de trabajo.

## Resend conectado (2026-08-27) ✅
- Se resolvió el límite de envío del correo por defecto de Supabase (ver nota de Capa 1 más arriba) configurando SMTP personalizado en Supabase Authentication → SMTP Settings, con Resend: host `smtp.resend.com` (typo inicial "smpt" corregido), puerto 587, usuario `resend`, remitente `onboarding@resend.dev` (dominio de pruebas de Resend — sin necesitar `duosync.app`, que sigue sin comprar). Verificado: el enlace mágico llega en segundos a `ideasmart.25m@gmail.com`.
- ⚠️ Limitación mientras no se compre el dominio propio: en este modo, Resend solo entrega correos a la MISMA cuenta con la que se registró en Resend (`ideasmart.25m@gmail.com`). Para que CUALQUIER cliente reciba su enlace de acceso, hay que comprar el dominio y verificarlo en Resend — pendiente, ya anotado en Problemas conocidos.
- ✅ Publicado (2026-08-27): commit `2f88173` "feat: conecta Gastos a Supabase y corrige paginas legales" subido a `main` y desplegado en Vercel — incluye páginas legales, login, Gastos/Categorías conectadas a Supabase, `app/lib/categorias.ts`, `app/lib/gastos.ts`.

## Conexión de datos reales — Gastos y Categorías (2026-08-27) ✅
- Preparación para el escaneo de recibos con IA (30-INTEGRACION-IA.md): antes de construir la IA, se conectó la pantalla Gastos a la base de datos real, porque el escaneo va a escribir directo en la tabla `expenses` — no tenía sentido construirlo sobre una pantalla que todavía mostraba datos de ejemplo.
- Nuevo `app/lib/categorias.ts` (mapa nombre-de-ícono → componente Lucide, ya que las categorías son personalizables y viven en la base de datos) y `app/lib/gastos.ts` (`obtenerCoupleId`, `listarCategorias`, `listarGastosDelMes`, `crearGasto` — todo contra Supabase real, respetando el RLS ya existente).
- `app/app/app/gastos/page.tsx` reescrita: categorías, lista de gastos, total del mes y el formulario "Nuevo gasto" ahora leen/escriben en Supabase de verdad (antes era estado local con `seed-datos.ts`, se perdía al recargar). Estados de carga (skeleton) y error agregados — antes no existían.
- Cuentas de Anthropic (para la IA) y clave secreta nueva de Supabase (para que el servidor escriba resultados de IA sin pasar por el navegador) ya configuradas por el usuario en `app/.env.local` — verificadas por presencia, nunca leí ni mostré su valor.
- ⚠️ INCIDENTE MENOR (2026-08-27): al verificar si `SUPABASE_SECRET_KEY` ya estaba guardada, un comando de búsqueda amplió el patrón y terminó mostrando el valor completo en el resultado de la herramienta. Se avisó de inmediato al usuario, quien revocó esa clave y generó una nueva. Lección reforzada (ya estaba la del incidente de la Sesión 6 con el service_role legacy): verificar la EXISTENCIA de una variable de entorno se hace con un patrón que solo confirma coincidencia (`files_with_matches`), nunca con uno que pueda imprimir el contenido.
- Alcance deliberado: Hoy y Metas siguen con datos de ejemplo — se conectan en una capa aparte, no bloquean el escaneo de recibos.
- Verificado: tsc ✓ build ✓. Consulta de sanity-check contra la base de datos real (con la clave secreta, sin imprimir su valor) confirmó que los nombres de columnas y tablas usados en el código coinciden con el esquema real (0 filas en `couples`/`categories`/`expenses` porque todavía nadie completó el registro real end-to-end). Verificación visual completa (crear un gasto y verlo persistir) PENDIENTE de que el usuario la haga en el sitio publicado, porque requiere iniciar sesión con su correo real.
- ✅ VERIFICADO END-TO-END con el usuario probando en el sitio real (2026-09-01): login con correo real, "Nuevo gasto" guarda de verdad en Supabase, persiste tras recargar, filtra por categoría correctamente.
- 🐛 BUG REAL encontrado y corregido durante la prueba: la política RLS de `select_own_membership` en `couple_members` se consultaba a sí misma (error Postgres 42P17 "infinite recursion detected in policy") — invisible en toda la Sesión 6 porque las pruebas anteriores usaban la clave de servicio (que se salta RLS), nunca una sesión real. Corregido con migración `20260901220000_fix_recursion_couple_members.sql`: función `security definer` `mi_couple_id()` que resuelve el couple_id sin volver a pasar por RLS, aplicada con `supabase db push` (sesión de CLI ya autenticada de antes, no hizo falta login interactivo).
- 🐛 BUG REAL encontrado y corregido: los primeros 4 gastos de prueba se guardaron todos como "Arriendo" sin importar qué categoría se tocara — causa real: el teclado numérico del celular envía "Enter/Listo" al escribir el monto, y si el usuario no había tocado la categoría todavía, el formulario se enviaba con la categoría por defecto. Corregido en `app/app/app/gastos/page.tsx`: el `Enter` en el campo de monto ya no envía el formulario solo.
- Dos incidentes de configuración resueltos en el camino (no relacionados con código): (a) el correo de acceso no llegaba por el límite bajísimo del proveedor de correo por defecto de Supabase — se conectó Resend en modo de prueba (sin dominio propio todavía, solo entrega al correo del propio usuario) vía SMTP Settings; (b) tras conectar Resend, el enlace de los correos apuntaba a `localhost` en vez del sitio real — Site URL/Redirect URLs de Supabase Authentication no se habían actualizado desde que se publicó en Vercel (pendiente ya anotado desde antes), corregido apuntando a `https://duosync-jfr5.vercel.app`.
## 🐛 CORREGIDO: el número de la meta no se actualizaba al aportar (2026-09-04)
- Reportado por el usuario: aportó $700.000, el % y la barra de progreso se movieron bien, pero el monto grande en negrilla se quedó congelado en el valor original.
- Causa real: `useCountUp` (la animación de "conteo" del número) tenía `useEffect(..., [])` — corría UNA sola vez al montar la pantalla y nunca más, así que cualquier cambio posterior a `montoActual` (como un aporte) actualizaba el % (que se calcula directo en cada render) pero no el número animado (que ya había terminado su única animación).
- Corregido: ahora anima cada vez que cambia el monto, desde el último valor mostrado hasta el nuevo — conserva la animación de entrada (0 → monto inicial al abrir la pantalla) y además reacciona a cada aporte nuevo.
- Verificado: tsc ✓ build ✓.

## Editor de categorías movido de Nosotros a Gastos (2026-09-04) — a pedido del usuario
- El usuario no encontró el editor en Nosotros y, al ubicarlo, pidió que quedara más cerca de donde se usan las categorías de verdad — regla de proximidad del propio sistema (12: "las acciones se ubican en el contexto de lo que afectan").
- `EditorCategorias` ya no vive en Nosotros (se quitó de ahí junto con la carga de categorías que solo servía para eso) — ahora es un enlace pequeño "Editar reparto y recurrencia" debajo de los filtros de categoría, en Gastos, reutilizando el `categorias` que esa pantalla ya tenía cargado.
- Verificado: tsc ✓ build ✓.

## 🐛 CORREGIDO: "Cuentas entre ustedes" decía "están al día" sin serlo (2026-09-04)
- Reportado por el usuario: registró un gasto con reparto 60/40, pero la tarjeta seguía diciendo "están al día — nadie le debe nada al otro".
- Diagnóstico verificado directamente contra la base de datos real (`supabase db query`, sin pasar por la app): la función `calcular_saldo_pareja()` SÍ calculaba bien el saldo real ($301.780, correcto según los gastos registrados) — el problema estaba en `app/lib/gastos.ts`: `obtenerSaldoPareja()` tenía una salvaguarda `if (ids.length < 2) return 0` para cuando la pareja todavía tiene un solo integrante (nadie con quien repartir) — y ESE es justo el caso real del usuario: todavía no se ha unido una segunda persona con el código de invitación. Esa salvaguarda devolvía 0, y la pantalla mostraba "al día" como si el saldo real fuera cero, cuando en realidad no hay nadie más registrado.
- Corregido: `obtenerSaldoPareja()` ahora devuelve `null` (distinto de un $0 real) cuando la pareja tiene un solo integrante, y Gastos muestra un mensaje honesto: "Cuando tu pareja se una con el código de invitación, aquí van a ver cuánto le corresponde a cada uno."
- Verificado: tsc ✓ build ✓ · el cálculo real de la función SQL se confirmó correcto contra la base de datos de verdad.
- ⚠️ Pendiente de que el usuario pruebe con su pareja de verdad unida a la cuenta (con el código de invitación) — solo así se puede ver el saldo real funcionando, ahora mismo la cuenta de prueba tiene un solo integrante.

## Reparto de gastos + saldo + recordatorios recurrentes (2026-09-03) ✅ — a pedido del usuario
Construcción grande en 2 partes, ambas completas.

**Parte 1 — dividir el gasto y llevar la cuenta:**
- `categories.split_percent` (default 50): % del gasto que le corresponde a quien lo REGISTRA; el resto es de su pareja. Editable por categoría (nuevo editor, ver abajo) y ajustable puntual por gasto (`expenses.split_percent`, nullable = usa el de su categoría) desde el mismo formulario de "Nuevo gasto" (control "Reparto: X% tú · Y% tu pareja").
- Tabla `settlements` + funciones `calcular_saldo_pareja()` / `liquidar_saldo()` (`20260903120000_reparto_y_saldo.sql`): el saldo se calcula SIEMPRE server-side desde la última liquidación (nunca sumando en el cliente, para que los dos vean la misma cuenta), usando `LEAST(user_id)/GREATEST(user_id)` como criterio determinístico de "quién es quién" (no depende de quién se unió primero). `calcular_saldo_pareja` verifica a mano que quien llama pertenece a esa pareja (es security definer, se salta RLS a propósito — sin este chequeo sería un IDOR).
- Gastos ahora tiene la tarjeta "Cuentas entre ustedes": cuánto le corresponde a cada uno y quién le debe a quién, con el botón "Ya nos pusimos al día" (liquida sin borrar el historial).
- `app/components/app/EditorCategorias.tsx` (nuevo, accesible desde Nosotros → "Categorías"): antes no existía NINGUNA forma de editar una categoría ya creada — ahora se puede ajustar el % de reparto y marcarla recurrente.

**Parte 2 — recordatorios de pago por correo:**
- `categories.es_recurrente` + `dia_vencimiento` (`20260903121500_recordatorios.sql`, con constraint que exige el día SOLO si es recurrente). Se activa desde el mismo Editor de Categorías.
- `app/app/api/cron/recordatorios/route.ts`: Vercel Cron (`app/vercel.json`, corre 1 vez al día, 13:00 UTC) revisa qué categorías vencen MAÑANA y le manda un correo a cada integrante de esa pareja vía Resend (SDK, no el SMTP de Supabase — esa vía es solo para los correos de autenticación). Protegido con `CRON_SECRET` (Vercel se lo manda solo en el header `Authorization` si esa variable existe — es su convención oficial). Idempotente: tabla `recordatorios_enviados` con `unique(category_id, año, mes)` evita mandar el mismo aviso dos veces si el cron corre de más.
- `app/lib/email/resend.ts`: el cliente de Resend se crea DENTRO de la función que lo usa, nunca al cargar el módulo — crearlo al importar rompía el build entero cuando la variable de entorno todavía no existía (defecto real encontrado al construir esto).
- Verificado: tsc ✓ build ✓ · confirmé contra la base de datos real que las columnas nuevas existen con sus valores por defecto correctos · las tablas `settlements`/`recordatorios_enviados` responden.
- ⚠️ PENDIENTE DEL USUARIO (sin esto, nada de lo de arriba funciona en producción): agregar en Vercel las variables `RESEND_API_KEY` (la misma clave que ya usan para el SMTP) y `CRON_SECRET` (cualquier texto largo que inventen, ej. una contraseña larga) — Production y Preview, y redeploy. Sin `RESEND_API_KEY` el cron corre pero falla al mandar el correo; sin `CRON_SECRET` cualquiera podría llamar al endpoint del cron desde afuera.
- ⚠️ MISMA LIMITACIÓN YA CONOCIDA de Resend (modo de prueba, sin dominio propio verificado): los correos de recordatorio probablemente solo lleguen de verdad a la cuenta con la que se registraron en Resend, no a cualquier pareja — se resuelve comprando el dominio y verificándolo ahí.
- Pendiente de que el usuario pruebe: no se pudo probar el flujo completo de correo end-to-end (el cron corre una vez al día; se puede probar llamando la URL a mano con el secreto una vez esté publicado).
- Alcance deliberado: sin recordatorio anticipado configurable (siempre avisa "mañana", no "3 días antes"); sin notificación push (decisión ya tomada con el usuario, correo primero).

## Landing: sección "¿Les suena?" con profundidad y color (2026-09-02) ✅ — a pedido del usuario
- Reportado: la lista de 4 preguntas se veía plana (chip gris "muted" del kit, tarjeta blanca sin relieve).
- `app/components/landing/Problema.tsx` (kit compartido): se agregó un prop opcional `color` por pregunta — sin tocar el comportamiento por defecto del kit (compatibilidad con cualquier otro uso). Con color: ícono en chip sólido de ese color, tarjeta con fondo tintado (10%), borde tintado (25%) y sombra tintada (nunca negra) — profundidad real sin caer en glow/neón (anti-slop, DESIGN-CORE).
- `app/app/page.tsx`: las 4 preguntas usan un color distinto cada una (`--cat-rose/amber/blue/violet`, los mismos tokens ya creados para las categorías de Gastos — reutilización, no una paleta nueva).
- Verificado: tsc ✓ build ✓ · captura real a 375px de las 4 tarjetas.

## Rediseño de categorías + Metas (2026-09-02) ✅ — a pedido del usuario
- **Categorías nuevas** (reemplazan las 5 anteriores): Arriendo/hipoteca 🏠 · Servicios públicos ⚡ · Supermercado 🛒 · Restaurantes/Salidas 🍕 · Transporte/Gasolina 🚗 · Entretenimiento 🎬. Migración `20260902180000_categorias_nuevas.sql`: cambia el set por defecto de `crear_pareja()` para parejas NUEVAS, y migra las categorías de la pareja de prueba ya existente al nuevo set (mismo criterio, nombre/ícono/color actualizados) para que no queden desincronizadas.
- **Un color propio por categoría** (antes solo alternaba entre 2): nuevos tokens `--cat-teal/coral/amber/rose/blue/violet/gray` en `tokens.css`, tonos desaturados de la misma familia que la marca (nada neón). Columna `categories.color` ampliada a esas 7 claves (antes solo `accent`/`accent-2`). `app/lib/categorias.ts` tiene el nuevo helper `colorDeCategoria()`.
- **Selectores más grandes**: el selector de categoría en "Nuevo gasto" pasó de una fila de pastillas chicas a una grilla de 3 columnas con ícono circular de 48px en su color propio — más fácil de tocar y de reconocer de un vistazo.
- **Categorías propias**: se puede agregar una categoría nueva desde el mismo formulario (botón "Otra") — el color se asigna solo, rotando la paleta.
- **Metas**: el nombre de la meta ya es editable (lápiz junto al título, antes fijo a "Viaje a Cartagena"). Los 4 íconos de progreso (semilla→árbol→árbol→fruto) usaban el MISMO ícono en el paso 2 y 3 (defecto real reportado) — ahora son 4 íconos distintos (Sprout→TreeDeciduous→Trees→Apple), se nota el crecimiento.
- Verificado: tsc ✓ build ✓ · migración aplicada y confirmada contra la base real · Metas revisada a 375px (captura real, con el mismo protocolo de desactivar/reactivar la protección de rutas en local).
- ⚠️ Pendiente de que el usuario pruebe: el selector de categorías nuevo en Gastos necesita una sesión real para verse con datos reales (no se pudo probar en local sin iniciar sesión) — falta la confirmación del usuario en el sitio publicado.
- Alcance deliberado: "Nueva meta juntos" sigue siendo un botón sin función (varias metas a la vez es una función más grande, aparte); Hoy sigue mostrando las categorías de EJEMPLO (seed-datos.ts), no las nuevas reales — se actualiza cuando se conecte Hoy a datos reales.

## Asistente de IA (2026-09-02) ✅
- Arquitectura (30-INTEGRACION-IA.md): texto corto/medio → SIEMPRE streaming (la respuesta aparece en vivo, no espera en blanco). `app/lib/ai/asistente.ts` arma el prompt con los gastos REALES del mes de la pareja (nombre de categoría, monto, fecha, nota — nunca inventa datos) + el historial de los últimos 10 mensajes. `app/app/api/asistente/route.ts` (BFF, 09-SEGURIDAD.md): trae los gastos vía RLS con la sesión del usuario (no hace falta la clave secreta para LEER datos que ya son suyos, a diferencia del escaneo de recibos que SÍ necesita escribir), llama a Claude Haiku 4.5 en modo stream y devuelve un `ReadableStream` de texto plano que el cliente va pintando chunk a chunk.
- UI: `app/components/app/AsistenteChat.tsx` — ventana de chat simple (burbujas usuario/asistente), botón flotante circular en Gastos (ícono robot) para abrirla.
- El asistente SOLO conoce los gastos de SU pareja (RLS lo garantiza en el fetch) y responde que no da asesoría financiera profesional si se lo preguntan (cumple lo prometido en `/aviso-ia`).
- Verificado: tsc ✓ build ✓ · llamada real de streaming probada de punta a punta (le di gastos de prueba y sumó correctamente: "1.712.000" = 512.000 + 1.200.000).
- ⚠️ Alcance deliberado (decisión técnica, documentada — no bloqueante): no se implementó un límite de uso propio por pareja (fair-use) ni el circuit-breaker de gasto global de `30-INTEGRACION-IA.md` — con Haiku 4.5 y conversaciones cortas de una sola pareja el costo es mínimo (fracciones de centavo por pregunta), pero si el uso crece o se abre a más usuarios, hay que agregar el tope diario/mensual en la cuenta de Anthropic (ya recomendado desde la sección del escaneo de recibos) y una tabla `ai_calls` para medir el gasto real.
- 🐛 CORREGIDO (2026-09-02, reportado por el usuario): el asistente solo veía los gastos del MES ACTUAL, así que no podía responder preguntas sobre meses anteriores. Ampliado a todo el historial (tope de 500 filas como salvaguarda), agrupado por mes en el contexto que recibe la IA — el volumen de gastos de un hogar es bajo, el costo en tokens sigue siendo mínimo.

## Escaneo de recibos con IA (2026-09-01) ✅
- Arquitectura (30-INTEGRACION-IA.md): asíncrono con sondeo (no cola/worker aparte — volumen bajo esperado para una pareja). Cliente sube la foto YA comprimida (`app/lib/imagen.ts`, máx 1400px/JPEG 75%) directo a Storage → crea fila en `receipt_scans` (estado pendiente) → avisa a `app/app/api/recibos/procesar/route.ts` → el servidor descarga la foto, arma la lista de categorías REALES de la pareja, llama a Claude Haiku 4.5 con salida forzada por tool-use (`app/lib/ai/receipt-scan.ts`, zod + reintento que reinyecta el error si la respuesta no valida) y escribe el resultado con la clave secreta (`app/lib/supabase/admin.ts` — nunca el cliente, tal como ya lo exigía la política de la tabla). El cliente sondea la fila cada 1.5s (máx ~20s) y abre el formulario de "Nuevo gasto" YA lleno con el monto/categoría sugeridos — el usuario SIEMPRE confirma o corrige antes de guardar (cumple lo prometido en `/aviso-ia`).
- Permisos nuevos: bucket `recibos` (creado por el usuario, privado) con políticas de Storage couple-scoped (`20260901223000_storage_recibos_rls.sql`), usando la misma función `mi_couple_id()` del fix de recursión para evitar el mismo problema.
- La categoría que sugiere la IA es SIEMPRE una de la lista real de categorías de la pareja (enum forzado en el esquema de la tool) — nunca puede inventar una categoría que no existe.
- Verificado: tsc ✓ build ✓ · llamada real a la API de Anthropic probada de punta a punta (confirmó correctamente que una imagen que no es un recibo no lo es) · costo real por escaneo: variables de entorno `ANTHROPIC_API_KEY`/`AI_MODEL=claude-haiku-4-5` configuradas, muy por debajo del 20% del precio de suscripción que exige la doctrina.
- ✅ VERIFICADO END-TO-END con una foto real del usuario (2026-09-02): leyó el recibo en ~3s, sugirió $39.520 en la categoría correcta (Mercado), el usuario confirmó y guardó desde el formulario pre-llenado.
- 🐛 Detalle real encontrado y corregido en el camino: `SUPABASE_SECRET_KEY` se había configurado solo en `.env.local` (desarrollo) pero nunca en las Environment Variables de Vercel — el endpoint fallaba en producción antes de tocar la fila (quedaba en estado "pendiente" para siempre). Agregada en Vercel (Production+Preview) + redeploy. Lección: cada variable de servidor nueva se agrega en AMBOS lugares desde el principio, no solo en local.
- ⚠️ BLOQUEADOR EN CURSO (2026-08-27): el límite de envíos del correo por defecto de Supabase (ya anticipado en la Sesión 6) impidió seguir probando el login. Se conectó Resend por SMTP personalizado en Supabase (Authentication → SMTP Settings, cuenta de Resend registrada con `ideasmart.25m@gmail.com`, remitente de prueba `onboarding@resend.dev`) — pero el enlace mágico SIGUE sin llegar tras la reconexión. Diagnóstico en curso: revisando los logs de envío de Resend para saber si Supabase siquiera intentó mandar el correo por ahí, o si el problema es la configuración SMTP en Supabase. Sin resolver esto no se puede seguir probando "Nuevo gasto" en Gastos ni avanzar al escaneo de recibos.

## 🐛 BUG REAL encontrado y corregido: el código de pareja por WhatsApp no traía el enlace (2026-09-02)
- Reportado por el usuario: la pareja que recibe el código por WhatsApp buscaba "DuoSync Wallet" en Play Store para poder entrar — no la encontraba porque la app NO es una app de tienda, es una web (decisión ya tomada, ver "Decisiones técnicas"). Causa real: el mensaje de WhatsApp (`app/app/onboarding/page.tsx`) solo mandaba el nombre de la app y el código de 4 dígitos, SIN el enlace del sitio — quien lo recibía no tenía forma de saber que debía abrir un navegador en vez de una tienda de apps.
- Corregido: el mensaje ahora aclara explícitamente "no hay que bajar nada de Play Store ni App Store" y agrega el enlace real del sitio (`window.location.origin`, para que apunte solo siempre al dominio donde esté publicada, sin hardcodear una URL que cambie cuando se compre el dominio propio).
- Pendiente de mejora futura (no bloqueante): el enlace lleva a la portada, no directo al paso de "unirse con código" con el código ya escrito — sería más fluido, pero no es necesario para que la pareja pueda entrar hoy.
- Verificado: tsc ✓ build ✓.

## País y moneda por pareja (2026-09-02) ✅
- Motivo: la app apunta a Colombia/México/Argentina (y el resto de LATAM), pero todo estaba hardcodeado a pesos colombianos. Se agrega selección de país FUERA del embudo de venta (onboarding/paywall son pantallas ya aprobadas, "cosa juzgada" — no se tocan).
- `couples.pais` (nuevo, nullable) + RPC `actualizar_pais_pareja(p_pais)` (`20260902150000_pais_pareja.sql`) — valida el código contra la lista fija de 19 países LATAM y que quien llama pertenece a esa pareja, mismo patrón que `crear_pareja`/`unirse_con_codigo`.
- `app/lib/paises.ts`: los 19 países con su moneda real y su locale de formato (México/Argentina/Colombia con su propio separador decimal, Brasil con `pt-BR`/R$, Ecuador/El Salvador/Panamá en USD porque es su moneda oficial real).
- `app/components/app/SelectorPais.tsx`: aparece UNA vez, apenas la pareja entra a la app y `couples.pais` sigue en null (se pregunta desde `app/app/app/layout.tsx`, visible en las 4 pestañas) — nunca vuelve a preguntar una vez elegido.
- Gastos (la única pantalla con datos reales por ahora) ya muestra los montos en la moneda elegida — antes usaba `$${n.toLocaleString('es-CO')}` fijo.
- Verificado: tsc ✓ build ✓ · probada la función de formato con México/Argentina/Colombia/Brasil, cada uno con su símbolo y separador correctos.
- ✅ AMPLIADO (2026-09-02, reportado por el usuario): eligió Brasil y "Hoy" seguía mostrando pesos colombianos — Hoy y Metas ahora también consultan el país real de la pareja y usan `formatoMoneda` para el saldo/meta, aunque los MONTOS que muestran siguen siendo de ejemplo (Mateo & Sofía) — es decir, el número es de mentira pero la moneda en la que se ve ya es la real. Cuando se conecten esas pantallas a datos reales, no hace falta tocar el formato de moneda otra vez.
- 🐛 BUG REAL encontrado y corregido (2026-09-02, reportado por el usuario): en Metas, "monto actual / monto objetivo" se escribía en una sola línea separados por "/" — funcionaba con pesos colombianos ($1.240.000 / $4.000.000) pero con el formato de Brasil (más largo, con decimales: R$1.240.000,00) el "/" se quedaba solo al final de la línea y el segundo monto se iba a su propia línea, pareciendo dos cifras sueltas sin relación. Corregido: ahora son dos líneas separadas con la palabra "de" y la etiqueta explícita "su meta total" — funciona sin importar el largo del número en cualquier moneda. Verificado a 375px con captura real (desactivando temporalmente la protección de rutas en local para poder verla sin sesión, restaurada de inmediato después — mismo protocolo ya usado en la auditoría de Sesión 6).
- Ajustado a pedido del usuario (2026-09-02): (a) la pregunta ahora dice "¿En qué país van a usar la app?" con aclaración explícita de que NO es el país donde están en ese momento (ej. de viaje) — antes decía "¿Desde qué país nos escriben?", ambiguo para alguien viajando; (b) se puede CORREGIR el país después: `app/app/app/nosotros/page.tsx` ahora tiene una fila real "País y moneda" (el primer dato real de esa pantalla, el resto sigue en datos de ejemplo) que reabre el mismo `SelectorPais` con una X para cerrar sin obligar a elegir — antes, una vez elegido, no había forma de cambiarlo.
- Verificado: tsc ✓ build ✓.

## Metas: monto de aporte editable (2026-09-02) ✅
- Reportado por el usuario: "Aportar" solo dejaba un monto fijo de $100.000, sin poder cambiarlo.
- Corregido en `app/app/app/metas/page.tsx`: el botón ahora abre un campo para escribir el monto que quieran aportar (mismo patrón visual que el formulario de Gastos — cancelar/confirmar, Enter no envía solo).
- Alcance deliberado (decisión del usuario): sigue en datos de ejemplo local, NO conectado a Supabase todavía — el aporte se ve y anima, pero se pierde al recargar. Conectar Metas a la base de datos real es la siguiente pieza natural, pendiente.
- Verificado: tsc ✓ build ✓.

## Cambio de nombre: DuoSync → DuoSync Wallet (2026-09-02) ✅
- Motivo: ya existe otra app llamada "DuoSync" (conflicto de nombre, decisión del usuario).
- Cambiado en TODO el código visible al usuario (34 menciones en 11 archivos: landing, onboarding, paywall, login, las 4 páginas legales, título de la pestaña del navegador) + el correo de contacto: `soporte@duosync.app` → `soporte@duosyncwallet.app`, `legal@duosync.app` → `legal@duosyncwallet.app` (el dominio real `duosync.app` NUNCA se compró, así que no se pierde nada comprando `duosyncwallet.app` en su lugar cuando llegue el momento).
- De paso corregido: la pregunta frecuente de la landing "¿Qué pasa si quiero cancelar?" también decía "desde su cuenta" (mismo error que ya se había corregido en Términos) — ahora dice correctamente "desde el área de miembros de Hotmart".
- NO se tocó: el nombre interno de las funciones/componentes en el código (`DuoSyncLanding`, etc. — son identificadores técnicos, no texto visible), el repositorio de GitHub (`duosync`), ni el proyecto de Vercel (`duosync-jfr5`) — renombrar esos tiene riesgo real de romper el despliegue automático y no aporta nada al usuario final; se puede hacer más adelante si el usuario lo pide explícitamente.
- ⚠️ Pendiente (acción del usuario, no código): actualizar el "Sender name" en la configuración SMTP de Supabase (Authentication → SMTP Settings) de "Duosync" a "DuoSync Wallet", para que el nombre del remitente en los correos también sea coherente.
- Verificado: tsc ✓ build ✓ · revisado a 375px en el navegador (header de landing y login no se rompen con el nombre más largo).

## Auditoría legal (2026-08-27) ✅
- Responsable: Gloria Alvarado, persona natural, Colombia. Correo legal: `legal@duosync.app` (⚠️ pendiente que el usuario confirme que esa bandeja ya recibe correo real — el dominio `duosync.app` mismo sigue pendiente de compra, ver "Problemas conocidos").
- Reescritas con contenido real (antes eran stubs genéricos): `app/app/privacidad/page.tsx` (subprocesadores nombrados: Supabase, Vercel, Hotmart; transferencia internacional declarada; borrado de cuenta con camino real por correo, 15 días hábiles; edad mínima 18; ley 1581 de Colombia), `app/app/terminos/page.tsx` (qué es/no es la app, renovación automática divulgada, cancelación real vía área de miembros de Hotmart — antes decía falsamente "desde su cuenta", que no existe —, licencia, responsabilidad, ley de Colombia), `app/app/reembolsos/page.tsx` (ya estaba coherente con 7 días de prueba + 15 de garantía, se le agregó el contacto correcto).
- Agregado aviso de consentimiento en el login (`app/app/login/page.tsx`): enlaza Términos y Privacidad en el punto de registro (antes no existía ningún aviso ahí).
- 🔴 HALLAZGO CRÍTICO corregido: `app/app/aviso-ia/page.tsx` describía el escaneo de recibos con IA y el asistente de IA como funciones YA ACTIVAS — no existe ninguna integración de IA en el código (verificado por grep: cero referencias a proveedores de IA). Reescrita para decir la verdad: "todavía estamos construyendo estas dos funciones, hoy no están activas".
- ⚠️ MISMO HALLAZGO, AÚN ABIERTO Y MÁS URGENTE: el paywall (`app/app/paywall/page.tsx:168`) y la landing (`app/app/page.tsx:126,140`) SIGUEN vendiendo "Escaneo de recibos con IA" como beneficio del plan Premium que se cobra HOY. Vender un beneficio que no existe es motivo real de reembolsos masivos y de que Hotmart marque la cuenta por publicidad engañosa. El usuario confirmó que sí van a construir estas dos funciones (no que se van a quitar de la venta) — pendiente decidir con el usuario si: (a) no se abre venta real hasta que el escaneo de recibos exista, o (b) se marca temporalmente "Próximamente" en paywall/landing mientras se construye. No se tocó el paywall/landing en esta auditoría porque es una decisión de negocio, no de redacción legal, y el paywall es una pantalla del dinero con gate de revisor-visual ya aprobado.
- Verificado: tsc ✓ build ✓.
- Cookies: no aplica todavía — no hay analytics, píxeles ni cookies no esenciales en el código (verificado por grep). Cuando se agregue analytics (36-ANALITICA-Y-EVENTOS.md), ahí sí hace falta el banner Aceptar/Rechazar.
- Datos financieros de la app (gastos, montos) revisados contra la nota de FICHA-MERCADO.md: NO son "datos sensibles" bajo la Ley 1581 (esa categoría es salud, origen étnico, orientación sexual, datos biométricos, etc.) — quedan protegidos por el régimen general de privacidad, no por el reforzado.

## Qué es esta app (3 líneas máximo)
App móvil para parejas en LATAM que combina finanzas compartidas (gastos, saldo común, metas de ahorro) con una dinámica diaria de conexión afectiva. Suscripción única por pareja (no por persona). Modelo freemium con trial.

## Promesa central
"Ayudo a parejas jóvenes de LATAM a organizar sus gastos compartidos y mantener la conexión diaria sin discusiones por dinero ni pagar dos suscripciones separadas."

## Reporte de validación (Sesión 1 — cerrado y re-verificado con datos de hoy)
- Origen: investigación de mercado que el usuario ya trajo hecha (DUOSYNC.docx, 87-91/100) + re-validación en vivo el 2026-08-12 (ver FICHA-MERCADO.md, fuentes con fecha).
- Veredicto: Excelente oportunidad — confirmado.
- Apps de referencia: Paired ($9.99-14.99/mes o $69.99-83.99/año por pareja, verificado 2026-08-12 — el dato de "cobro doble por persona" del docx original NO se pudo confirmar hoy y no se usa como afirmación en el copy de venta), Agape (~$15/año o freemium, dato sin re-verificar), Splitwise/Monefy (gratis con anuncios o ~$9.99/año, dato sin re-verificar).
- Lo que sí es verificable y es nuestra ventaja real: DuoSync cuesta 50-65% menos que Paired, cobra en moneda local vía Hotmart (con PSE/OXXO/PIX), y combina gasto del hogar + conexión afectiva en un solo lugar — algo que ninguna referencia hace junta.
- Brecha LATAM confirmada: sí — sin versión localizada fuerte, precios de referencia en USD.
- Precio de referencia del mercado: $9.99-14.99 USD/mes por pareja (Paired, la más cercana) → DuoSync: $4.99/mes o $29.99/año por pareja.

## Dirección de Arte (Sesión 2 — CERRADA, cosa juzgada)
- FICHA-ARTE.md: existe y aprobada — 2026-08-12
- ¿Hubo referencia visual del usuario?: NO — se presentaron 3 fusiones (A/B/C) y el usuario combinó B (base) + elementos de C
- Resumen: fondo #F3F7F6 · acento #1C4F49 (verde azulado) · acento 2 #FF6B47 (coral) · Display "Schibsted Grotesk/Archivo" · Body "Instrument Sans" · radio 14-22px · modo claro
- Personalidad: confiable · ordenada · cálida-en-los-acentos
- Composición elegida: pregunta del día como tarjeta-hero arriba de la pantalla (dispositivo ownable) + saldo con monto y % de la meta + categorías en filas con barra de progreso
- REGISTRO ANTI-REPETICIÓN: verde azulado #1C4F49 + coral #FF6B47 + par Schibsted Grotesk/Instrument Sans vetados para el próximo proyecto del SO

## Avatar y venta (Sesión 1)
- FICHA-AVATAR.md: existe, APROBADA (2026-08-12 — el usuario amplió el rango de edad a 18-45 y precisó qué espera de la app; ya incorporado).
- Resumen: parejas 18-45 años que comparten arriendo/hipoteca, servicios y alimentación · dolor #1: la carga de "recargarle" al otro o los olvidos de pago desgastan la relación · deseo #1: manejar el gasto del hogar de forma justa y ordenada sin pelear · nivel de consciencia: consciente del problema y de soluciones existentes (Paired/Agape/Splitwise) pero las ve caras, rígidas o incompletas.

## Estrategia de monetización (decidido y confirmado con FICHA-MERCADO.md — cosa juzgada)
- Modelo: Freemium con paywall tras la primera dinámica (onboarding-first, no hard paywall) — el usuario necesita vivir el "aha" de vincularse y ver el resultado antes de pagar; un hard paywall mataría la viralidad orgánica que es el canal #1 (TikTok/Reels).
- Trial: 7 días gratis + 40% de descuento en el anual como oferta de lanzamiento.
- Pricing final (ajustado por el usuario 2026-08-14): $5.99 USD/mes (menos de $0.20/día) o $35.94 USD/año = $2.99/mes (menos de $0.10/día, ahorro >50% vs. mensual) por pareja — 40-60% más barato que la referencia del mercado (Paired, $9.99-14.99/mes), verificado en FICHA-MERCADO.md. El copy público YA NO nombra competidores por nombre (decisión del usuario): se compara por beneficio/precio en general, nunca citando "Paired" u otra app.
- Free: vinculación de cuentas + 1 pregunta diaria + hasta 15 movimientos/mes. Premium: historial ilimitado, gráficos, panel personalizable, escaneo de recibos con IA, asistente de IA, categorías ilimitadas, catálogo completo de dinámicas, metas de ahorro.
- Pasarela: Hotmart — acepta tarjeta, PayPal, PSE (Colombia), OXXO (México), PIX (Brasil) y cuotas hasta 12x. Importante para el diseño de cobros fallidos (dunning): los pagos con PIX/boleto NO se auto-cobran, generan un código nuevo cada renovación con 48h para pagar — se diseña un recordatorio para no perder clientes por esto (se retoma en la Sesión 6/8).
- Garantía: 15 días (verificado — Hotmart permite 7/15/21/30, se eligió 15 por ser mayor que el trial de 7, ver FICHA-MERCADO.md §4). Se configura en el panel real de Hotmart en la Sesión 6.

## Gamificación y retención (a diseñar en Sesión 4)
- Loop propuesto: Gatillo (notificación asincrónica cuando la pareja responde/registra un gasto) → Acción (responder pregunta diaria o registrar gasto en 2 toques) → Recompensa (revelar coincidencia / ver balance actualizado en vivo) → Inversión (racha de días conectados + metas de ahorro compartidas).
- Primera victoria (<5 min): vincular los dos teléfonos con código de 4 dígitos y completar el primer test exprés que revela puntos de coincidencia y el saldo común.

## Secuencia maestra de construcción (NO saltar)
- Estado de la secuencia: nada construido todavía.
- Ruta aprobada: `/` → `/onboarding` → `/paywall` → `/login` → `/app`
- Landing: CONSTRUIDA (código en `app/app/page.tsx`, componentes en `app/components/landing/`, copy en `docs/copy/landing.md`) — protagonista: el hero "Dejen de ser socios de una empresa en quiebra" — CTA unificado: "Vincular con mi pareja gratis" → `/onboarding`
- Onboarding: CONSTRUIDO (código en `app/app/onboarding/page.tsx`, UI compartida en `app/components/onboarding/ui.tsx`) — 9 pasos: 5 preguntas + reconocimiento + vinculación (código de 4 dígitos, con envío por WhatsApp) + loading (con cancelar) + resultado
- Paywall: CONSTRUIDO (código en `app/app/paywall/page.tsx`) — single-page (blueprint C1), $5.99/mes o $35.94/año, garantía 15 días, cabe completo en 375×812 sin scroll
- Login/Auth: CONSTRUIDO (código en `app/app/login/page.tsx`) — UI de magic link (Supabase Auth real se conecta en Sesión 6), pantalla secundaria (no una de las 4 del dinero)
- App interna: CONSTRUIDA (2026-08-16) — shell con bottom-nav en `app/app/app/layout.tsx` (rutas `/app/hoy`, `/app/gastos`, `/app/metas`, `/app/nosotros`), datos semilla realistas en `app/lib/seed-datos.ts` (misma pareja Mateo & Sofía y mismos montos que la landing — consistencia entre pantallas). Hoy: pregunta diaria con mecánica de revelar-al-responder-ambos, saldo del mes animado, top categorías, acceso a registrar gasto, teaser de meta con barra de progreso. Gastos: lista con filtros por categoría, navegación real entre meses, formulario de alta funcional (estado local). Metas: progreso con la metáfora semilla→árbol, botón de aportar. Nosotros: racha con calendario de 28 días, catálogo de dinámicas marcado honestamente "Próximamente". Probado a mano: responder pregunta revela ambas respuestas, agregar gasto actualiza el total en vivo, navegación entre meses funciona.
- Servicios externos: pendiente

## Puertas de etapa
- Landing: construida y verificada a mano — última revisor-visual 2026-08-15: 30/40 · 14/20 · 15/20 (NO LISTA formal, defectos restantes menores, ver Problemas conocidos) — evidencia: tsc ✓ build ✓ dev ✓ · docs/revisiones/landing-375.png
- Onboarding: construido y verificado a mano — última revisor-visual 2026-08-15: 28/40 · 14/20 (NO LISTA formal, defectos restantes menores) — evidencia: docs/revisiones/onboarding-375.png, onboarding-vinculacion-375.png, onboarding-loading-375.png
- Paywall: construido y verificado a mano — última revisor-visual 2026-08-15: 29/40 · 12/20 · 15/20 (NO LISTA formal; el único bug funcional real —CTA fuera de pantalla— ya corregido) — evidencia: docs/revisiones/paywall-375.png
- Login/Auth: construida (pantalla secundaria, sin gate de revisor obligatorio) — evidencia: tsc ✓ build ✓ dev ✓ · probada a mano
- App interna: construida y verificada a mano — pantalla principal (Hoy) 2 rondas de revisor-visual: 1ª 28/40·12/20 (NO LISTA, 5 defectos corregidos: barra de progreso de meta, saldo animado, editar respuesta, profundidad de fondo, empty state) → 2ª 29/40·14/20 (NO LISTA, craft subió pero aparecieron 2 defectos nuevos, ya corregidos: navegación con `<a>` nativo reemplazada por `next/link` en toda la app interna — antes cada tap entre pestañas recargaba la página completa, confirmado con medición real que ya no ocurre —, botón "Responder" competía en peso visual con "Registrar gasto" y quedaba con opacity-50 muerto antes de escribir, ambos corregidos). Pendiente: 3ª pasada de revisor-visual no lanzada todavía (costo ~70k tokens) — evaluar con el usuario si se corre o se documenta lo que quede y se avanza. Gastos/Metas/Nosotros son pantallas secundarias del mismo tipo (bottom-nav, mismos tokens), sin gate de revisor obligatorio.
- Servicios externos: bloqueados

## Decisiones técnicas (NO re-discutir sin pedirlo el usuario)
- Framework: Next.js (decisión del agente — por SEO de la landing, rutas server-side para el paywall y porque hace posible que la misma app funcione como web instalable en iPhone y Android sin construir dos apps nativas separadas, que es lo que pidió el usuario).
- Plataforma: web app responsive e instalable (PWA) — se ve y se siente como una app nativa en ambos sistemas operativos, sin pasar por App Store/Play Store (evita el costo y tiempo de una app nativa; se revisa más adelante si conviene publicarla también en las tiendas).
- MVP (ampliado el 2026-08-12 con lo que el usuario definió que esperan sus suscriptores):
  1. Vinculación de pareja por código de 4 dígitos, configuración inicial en menos de 5 minutos.
  2. Registro de gastos del hogar (arriendo/hipoteca, servicios, alimentación) con categorías personalizables (no solo las de fábrica) y balance en vivo, sincronizado al instante entre los dos teléfonos.
  3. Panel de inicio personalizable (arrastrar y ordenar qué se ve primero).
  4. Escaneo de recibos con IA para registrar gastos sin teclear.
  5. Asistente de IA dentro de la app (su función exacta se define en Sesión 1 — candidatas: resolver dudas sobre el gasto del mes, sugerir cómo dividir un gasto puntual, resumir el mes).
  6. Metas de ahorro colectivas.
  7. Pregunta diaria de conexión (respuestas bloqueadas hasta que ambos respondan) — se mantiene como el gancho de uso diario y de retención.
- Arquitectura de IA (a definir en Sesión 1 con 30-INTEGRACION-IA.md): el escaneo de recibos necesita procesamiento de imagen — probablemente asíncrono (se sube la foto, la IA la procesa en segundo plano, el usuario ve el resultado en segundos) para que la app no se sienta lenta.
- Fuera del MVP (decisión del agente, a confirmar con el usuario): integración bancaria automática, chat/voz integrado, mapas de citas, sistema de recompensas complejo.
- Qué la app NUNCA debe hacer (decisión del agente basada en el nicho — dato sensible: finanzas + pareja): nunca compartir los datos financieros de un miembro de la pareja fuera de la cuenta vinculada sin su consentimiento explícito, nunca inventar o estimar montos que el usuario no ingresó (ni con la IA del escaneo: si no está seguro, se lo pregunta al usuario), nunca usar culpa o comparación entre los dos miembros como mecánica de retención, nunca mostrar anuncios ni cobrar nada fuera del precio pactado.

- Auth: Supabase Auth con magic link por correo como método principal (sin contraseña que olvidar) + opción de entrar con Google. La vinculación de pareja (código de 4 dígitos) es una capa propia sobre el login, no reemplaza el auth.
- Modelo de datos (resumen — el detalle de columnas/índices se escribe como migración en la Sesión 6): `profiles` (1 por usuario) · `couples` (1 por pareja, con código de invitación, estado del plan y fecha de fin de trial) · `couple_members` (une usuario↔pareja) · `categories` (propias de cada pareja, personalizables) · `expenses` (monto en numeric, nunca float, categoría, quién lo registró, recibo opcional) · `receipt_scans` (procesamiento async de la foto del recibo por IA) · `savings_goals` · `daily_questions` + `daily_answers` (bloqueadas hasta que ambos respondan) · `streaks` · `dashboard_layout` (el orden personalizable de cada usuario). RLS en TODAS: un usuario solo ve filas de la pareja a la que pertenece.
- Arquitectura de IA: el escaneo de recibos es ASÍNCRONO (se sube la foto, se procesa en segundo plano, se notifica cuando está listo) para que la app nunca se sienta trabada; el asistente de IA dentro de la app es SÍNCRONO tipo chat (respuesta en streaming). Detalle completo en Sesión 1 con `30-INTEGRACION-IA.md` al escribir el código.

## Sesiones completadas ✅
- Sesión 1 — Validación re-verificada, FICHA-AVATAR.md aprobada, FICHA-MERCADO.md creada, monetización/pricing/pasarela fijados, modelo de datos + RLS + auth + arquitectura de IA decididos — verificado 2026-08-12
- Sesión 2 — Identidad visual: 3 direcciones A/B/C presentadas y comparadas a 375px, usuario eligió combinación B+C, FICHA-ARTE.md cerrada y aprobada — 2026-08-12
- Sesión 3 — Landing construida desde el kit canónico (Next.js en `app/`), tokens tematizados, copy en `docs/copy/landing.md`, páginas legales stub creadas — verificado 2026-08-13
- Sesión 4 — Onboarding (9 pasos), paywall (single-page) y login (magic link) construidos; 7+ rondas de revisor-visual con correcciones reales; cerrada por decisión del usuario con pulido menor pendiente para Sesión 7 — verificado 2026-08-15

## Sesión en progreso 🔧
- Sesión 5 — App interna simplificada (Hoy · Gastos · Metas · Nosotros)
- ⚠️ INCIDENTE DE SEGURIDAD (2026-08-27): al verificar si `SUPABASE_SECRET_KEY` ya estaba guardada en `app/.env.local`, se usó un comando que imprimió el valor completo de la clave (quedó visible en el historial de esta conversación). Se avisó al usuario de inmediato; se le pidió eliminar esa clave en Supabase y generar una nueva. Lección (mismo tipo de error que el incidente del 2026-08-24): verificar la EXISTENCIA de un secreto solo con comandos que confirman presencia (ej. `grep -l`), nunca con modos que muestren el contenido de la línea.
- Sesión 6 (continúa, 2026-08-27) — Escaneo de recibos con IA: investigado y decidido el proveedor (Claude Haiku 4.5 vision, ~$0.003/recibo, mismo proveedor que el resto del SO — descartados los OCR especializados tipo Veryfi/Mindee/Taggun por costo desproporcionado a este precio de suscripción). Usuario ya creó cuenta en console.anthropic.com y guardó `ANTHROPIC_API_KEY` + `AI_MODEL=claude-haiku-4-5` en `app/.env.local`. Pendiente que el usuario cree el bucket privado `recibos` en Supabase Storage y guarde `SUPABASE_SECRET_KEY` (la nueva, no la Legacy) en el mismo `.env.local` — con eso se empieza a escribir el código (tabla `receipt_scans` ya existe en el esquema desde la Sesión 6 original, con su RLS ya lista para este flujo: el cliente solo puede insertar/leer, el resultado lo escribe el servidor con la secret key).

## Próximas sesiones 📋
- Sesión 6: Servicios externos (Supabase, auth real, Hotmart, dominio, Resend)
- Sesión 7: Pulido de landing/onboarding/paywall (defectos menores documentados abajo)

## Logo (2026-08-20)
- El usuario trajo su propio isotipo (infinito azul/dorado con signos de peso) generado con Gemini. Archivo original guardado en `docs/assets/logo/duosync-isotipo-original.jpg` — resultó ser un JPEG sin transparencia real (el "fondo transparente" que se veía en el chat era un patrón de cuadros pintado en los píxeles, no canal alfa). Se limpió con un script propio (Node + sharp, sin dependencias nuevas en el repo — se instaló y se desinstaló en una carpeta temporal fuera de `app/`): se reemplazó el patrón de cuadros por el `--bg` real de la app (#F3F7F6) y se recortó al bounding box de la marca.
- Assets finales: `app/public/logo-duosync.png` (234×128, para headers) · `app/app/icon.png` (512×512) · `app/app/apple-icon.png` (180×180) — Next.js los sirve automáticamente como favicon/apple-touch-icon sin configuración adicional.
- Colocado en: header de la landing (`Hero`), footer de la landing (`FooterLegal`), y el header compartido del funnel de onboarding/paywall/login (`FunnelHeader` en `app/components/onboarding/ui.tsx`) — reemplazó el cuadrado de color liso que hacía de placeholder. De paso se corrigieron 2 enlaces `<a href="/">` a `next/link` en `FunnelHeader` (mismo defecto de recarga completa que ya se había corregido en la app interna).
- CERRADO 2026-08-20: el usuario confirmó recolorear el azul del isotipo real al verde azulado de FICHA-ARTE.md (#1C4F49), manteniendo el dorado — hecho con un script propio (Node + sharp, temporal) que reasigna el matiz solo de los píxeles azules (rampa suave por "azulado", no umbral binario, para que el borde no quede entrecortado) y deja el dorado intacto. El nombre "DuoSync" en los headers ahora usa el coral de la marca (`text-[var(--accent)]` en `Hero.tsx`). Archivo final: `docs/assets/logo/duosync-isotipo-verde.png` (fuente de los 3 assets integrados). El símbolo de $ y las monedas del centro se agrandaron ~30-45% (pedido del usuario: se veían chicos) recortando esa región, escalándola y re-componiéndola con una máscara radial difuminada para que no se note el parche sobre la cinta.
- Reference file conservado en `docs/assets/logo/duosync-isotipo-trimmed.png` (recorte sin fondo, colores ORIGINALES azul/dorado, antes de recolorear) por si se necesita volver atrás.
- REEMPLAZADO 2026-08-21: el usuario volvió a Gemini y generó una versión nueva ya en verde/dorado con mejor nitidez (`docs/assets/logo/duosync-isotipo-v2-original.jpg`, formato "Viridian"). Se limpió el cuadriculado de fondo (JPEG sin transparencia real, igual que la v1) y se usó esta como fuente final — reemplaza a `duosync-isotipo-verde.png` en los 3 assets integrados (`app/public/logo-duosync.png` 233×128, `app/app/icon.png`, `app/app/apple-icon.png`). No hizo falta agrandar el $/monedas a mano esta vez, ya vienen más grandes y con brillos propios en el archivo de Gemini. Queda un ruido de compresión JPEG leve alrededor del signo de pesos en el archivo fuente (`duosync-isotipo-v2-trimmed.png`) — invisible al tamaño real que se usa en la app (~128px), no se intervino más.
- ⚠️ Nota de estrategia (no bloqueante, avisada al usuario): el H1/subtítulo/CTA del Hero de la landing se reemplazaron el 2026-08-20 a pedido explícito del usuario por copy más genérico ("Gestión de gastos juntos, sin hojas de cálculo ni momentos incómodos") que ya NO usa el ángulo específico de pareja ("socios de una empresa en quiebra") derivado de FICHA-AVATAR.md. El copy anterior estaba trazado a las entrevistas del avatar; el nuevo es funcional pero genérico (serviría para cualquier gasto compartido, no solo parejas). Si la conversión baja, este es el primer lugar a revisar. CTA_LABEL de toda la landing (Hero/Oferta/sticky/CTA final) unificado a "Empieza a simplificar tus cuentas".
- 2026-08-21: a pedido del usuario, se le dio profundidad (sombra tintada, no glow/glass) y un tono más visible a 3 "recuadros de diálogo" que quedaban demasiado planos: (1) las burbujas de respuesta M/S dentro de "Pregunta de hoy" en `app/app/app/hoy/page.tsx` (mezcla de --bg subida de 14%→24%, más sombra oscura ya que están sobre una tarjeta oscura), (2) la tarjeta-hero de las preguntas del onboarding en `app/app/onboarding/page.tsx` (`shadow-[var(--shadow-2)]` + color-mix con 8% de blanco), (3) las respuestas del FAQ en `app/components/landing/Faq.tsx`, que antes eran texto plano sin caja — ahora tienen su propia tarjeta blanca con `--shadow-1`.
- 2026-08-21: usuario pidió (con foto de referencia) un menú flotante para la app interna y más color en los íconos de categoría de gastos. Se rediseñó `app/app/app/layout.tsx`: la barra inferior pasó de franja plana de ancho completo a una píldora flotante (separada de los bordes, `shadow-2`, destino activo = círculo sólido coral) — igual en ESTRUCTURA a la referencia, pero en los colores claros de FICHA-ARTE en vez del negro de la foto (avisado al usuario, pendiente de confirmar si prefiere la versión oscura). Los íconos de categoría en `app/app/app/gastos/page.tsx` (lista completa) y `app/app/app/hoy/page.tsx` (top 3 categorías) pasaron de fondo tintado al 12% a relleno sólido del color de la categoría con ícono en `--bg`.

## Problemas conocidos ⚠️
- Legal: `legal@duosync.app` está escrito en las páginas legales pero el dominio `duosync.app` todavía no está comprado (mismo pendiente que `soporte@duosync.app`, ver nota de Landing más abajo) — confirmar que ambas bandejas reciben correo real antes de anunciar el correo legal a usuarios.
- Legal: paywall y landing venden "Escaneo de recibos con IA" como beneficio Premium activo; la función no existe en el código todavía (ver Auditoría legal arriba). Pendiente decisión del usuario antes de vender de verdad.

- Veredicto landing: docs/revisiones/landing-veredicto.md dice "Veredicto: NO LISTA" (30/40 · 14/20 · 15/20, 2026-08-15). Pospuesto a propósito: el usuario vio el resumen de esta ronda y decidió avanzar a Sesión 5 en vez de seguir iterando (rendimientos decrecientes tras 7+ rondas). Los defectos que quedan son de pulido visual, no bugs — quedan listados abajo y se retoman en Sesión 7.
- Veredicto onboarding: docs/revisiones/onboarding-veredicto.md dice "Veredicto: NO LISTA" (28/40 · 14/20, 2026-08-15). Mismo pospuesto por decisión del usuario — defectos de pulido visual listados abajo, se retoman en Sesión 7.
- Veredicto paywall: docs/revisiones/paywall-veredicto.md dice "Veredicto: NO LISTA" (29/40 · 12/20 · 15/20, 2026-08-15). Mismo pospuesto por decisión del usuario — el único bug funcional real que encontró esa ronda (CTA fuera de la pantalla a 375×812) SÍ se corrigió antes de posponer; lo que queda es pulido visual, listado abajo, se retoma en Sesión 7.
- Veredicto app-hoy: docs/revisiones/app-hoy-veredicto.md dice "Veredicto: NO LISTA" (29/40 · 14/20, 2026-08-16, 2ª ronda). Los 2 bugs reales de esa ronda (navegación con `<a>` nativo rompiendo la app-feel, botón "Responder" compitiendo con el CTA principal) ya se corrigieron. Lo que queda (heurística 9 sin estado de error modelado) se pospone a propósito: sin backend real todavía, un "error" sería artificial — se aborda en Sesión 6 cuando el envío de la respuesta pase por una llamada de verdad.

Pulido menor pendiente para Sesión 7 (no bloqueante — ninguno es un bug funcional; el único que lo era, el CTA del paywall fuera de pantalla, ya se corrigió el 2026-08-15):
- Landing: la sección "App por dentro" muestra placeholders (Hoy/Gastos/Metas/Nosotros) en vez de screenshots reales — aceptado hasta que la app interna exista (Sesión 5); reemplazar antes de mandar tráfico pago.
- Landing: soporte@duosync.app es un correo/dominio provisional — actualizar cuando el usuario compre el dominio real (fase de servicios externos).
- Landing: CTA sin prefetch de next/link; alternancia base/elevado entre secciones podría reforzarse; falta footnote/fuente para la cifra "60% más barato" en la prueba social.
- Onboarding: el paso de vinculación tiene 8 elementos apilados sin mucha jerarquía entre sí (ícono, título, párrafo, código, WhatsApp, nota, link alterno, CTA) — funciona pero podría respirar mejor.
- Onboarding/Paywall/Landing: profundidad visual (fondos, mesh) sigue leyéndose plana en algunas zonas pese a varios ajustes — el mesh del paywall se reposicionó y reforzó el 2026-08-15, evaluar con ojos frescos en la próxima ronda si ya es suficiente.
- Paywall: en viewports más cortos que 812px (ej. 740px, común en Android con navegador con barra), el contenido todavía desborda ~66px y pide un scroll corto para llegar al CTA — a 375×812 (el estándar de referencia) ya cabe completo y sin scroll.
- DESVIACIONES DEL KIT de landing pedidas explícitamente por el usuario el 2026-08-14 (documentadas per doctrina de plantillas-codigo/landing/README.md): `Oferta.tsx` usa `MarkedCopy` en vez de texto plano para negrillas; `AppPorDentro.tsx` tiene autoplay cada 3.2s (pausable, respeta reduced-motion); `HeroVisual.tsx` tiene la metáfora semilla→árbol de la meta de ahorro.
- LINTER DE DISEÑO en `AppPorDentro.tsx`: marca valores del kit original (`w-[250px]`, `rounded-[30px]`, etc.) como fuera de escala — son del kit, no de esta sesión, y ya fueron aprobados por el revisor-visual en 6 pasadas. No se tocan (doctrina: "no se re-estiliza el kit").
- Token semántico `--danger: #d9483a` agregado en `tokens.css` para reemplazar hex directo repetido en onboarding/paywall (lo detectó el linter de diseño).
- App interna: en Gastos se corrigió un bug real (no solo pulido) el 2026-08-16 — el gasto del día 1 de cada mes desaparecía del total por parsear fechas ISO con `new Date()` (se interpreta como UTC y en timezones detrás de UTC cae en el mes anterior); ahora compara por prefijo de string, verificado con los 8 gastos semilla mostrando el total correcto ($2.140.000, igual que en Hoy).
- App interna: Metas y Nosotros no pasaron por revisor-visual (pantallas secundarias del mismo tipo que Hoy, ya aprobado — doctrina: solo la primera de cada tipo nuevo lo requiere); verificadas a mano y consistentes en tokens/spacing con Hoy.

## Pendientes del usuario (acciones que el usuario debe hacer)
- [ ] Ninguno todavía — se avisa cuando lleguemos a servicios externos (cuentas de Supabase, Vercel, Hotmart, dominio).

## Notas para la próxima sesión
- El usuario ya trajo una investigación de mercado propia y completa (guardada en DUOSYNC.docx) — no repetirla desde cero, solo re-validar con datos actuales y llenar FICHA-MERCADO.md con fuentes verificables antes de fijar precio/garantía en definitivo.
